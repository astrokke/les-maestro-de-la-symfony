{% extends 'base_admin.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block main %}
    <h2>{{ title }}</h2>
<div class="row mt-4">
                <div class="col-md-6 mb-4">
                    <div class="card">
                        <div class="card-body ">
                            <strong>Id :</strong> {{ users.id }} <br>
                            <strong>Nom :</strong> {{ users.nom }} <br>
                            <strong>Prénom :</strong>  {{ users.prenom }}<br>
                            <strong>Addresse mail :</strong>  {{ users.email }}<br>
                        </div>
                    </div>
                </div>
    </div>
        <a href="{{ path('app_user_list_admin') }}" class="btn btn-secondary">Retour</a>
{% endblock %}