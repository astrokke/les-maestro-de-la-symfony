{% extends 'base.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block main %}
    <h2>{{ title }}</h2>
        <p>
            <br>{{ users.id }}</br>
            <br>{{ users.nom }}</br>
            <br>{{ users.prenom }}</br>
            <br>{{ users.email }}</br>
        </p>
{% endblock %}