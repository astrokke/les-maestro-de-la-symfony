{% extends 'base.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block main %}
    <h2>{{ title }}</h2>
        <p>
            {{ admin.id }}-{{ admin.username }}-{{ admin.nom }}-{{ admin.prenom }}
        </p>
{% endblock %}