<form method="post" action="{{ path('app_delete_admin', {'id': item.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cet administrateur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item.id) }}">
    <button class="btn">Supprimer</button>
</form>
