{% extends 'base.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block main %}
    <h2>{{ title }}</h2>
        <p>
            {{ produit.id }}-{{ produit.libelle }}-{{ produit.description }}-{{ produit.prixHt }}
        </p>
{% endblock %}