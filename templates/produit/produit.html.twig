{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block main %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div>
    <h1>Mes produits</h1>
    <a href="{{ path("app_index") }}">retour home</a>
    <a href="{{ path("app_createProduit") }}">Lien pour crée un article</a>
</div>

<div>
    {% for produit in produits %}

   <a href="{{ path('app_produit', {id: produit.id}) }}">{{ produit.libelle }}</a>
   
    {% endfor %}
    
</div>


{% endblock %}