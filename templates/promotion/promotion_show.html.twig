{% extends 'base.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block main %}
    <h2>{{ title }}</h2>
        <p>
            <br>{{ promotion.id }}</br>
            <br>{{ promotion.libelle }}</br>
            <br>{{ promotion.tauxpromotion }}</br>
            <br>{{ promotion.codepromotion }}</br>
        </p>
{% endblock %}